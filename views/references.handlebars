<section class="clients-hero hero is-medium"><br><br><br>
    <div class="hero-body remove-padding">
        <div class="container reduced has-text-centered">
            <h1 class="title h1HP is-red is-heavy boost-margin">{{{__ "currentformerclients"}}}</h1>
            <hr>{{>clients-min}}
        </div>
    </div>
</section>

<section class="consultancy-hero hero is-large-intro wave-bottom-offwhite">
    <section class="testimonials-hero hero is-medium">
        <div class="hero-body remove-padding">
            <div class="container reduced has-text-centered">
                <h2 class="title h1HP is-red is-heavy boost-margin">{{{__ "testimonialsTitle"}}}</h2>

                <div class="columns is-multiline has-text-left">
                    {{>testimonials-min}}


                    
                </div>
            </div>
        </div>

    </section>
</section>